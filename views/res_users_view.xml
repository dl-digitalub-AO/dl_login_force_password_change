<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form" model="ir.ui.view">
        <field name="name">res.users.form.must.change.password</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='login']" position="after">
                <label for="must_change_password" string="Password"/>
                <div>
                    <field name="must_change_password" class="oe_inline"/>
                    <button
                        name="action_reset_password"
                        string="Force Password Change on Next Login"
                        type="object"
                        class="oe_link"
                        invisible="must_change_password"
                    />
                </div>
            </xpath>
        </field>
    </record>
</odoo>